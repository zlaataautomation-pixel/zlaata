<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
    <configuration>

        <!-- report theme -->
        <!-- STANDARD, DARK -->
        <theme>STANDARD</theme>

        <!-- document encoding -->
        <!-- defaults to UTF-8 -->
        <encoding>UTF-8</encoding>

        <!-- protocol for script and stylesheets -->
        <!-- defaults to https -->
        <!-- HTTP, HTTPS -->
        <protocol>HTTPS</protocol>

        <!-- offline report -->
        <timelineEnabled>true</timelineEnabled>

        <!-- offline report -->
        <enableOfflineMode>false</enableOfflineMode>

        <!-- use thumbnails for base64 images -->
        <!-- this may slowdown viewing tests -->
        <thumbnailForBase64>false</thumbnailForBase64>

        <!-- title of the document -->
        <documentTitle>Zlaata QA-Framework</documentTitle>

        <!-- report name - displayed at top-nav -->
        <reportName>Zlaata_Automation_Reports</reportName>

        <!-- timestamp format -->
        <timeStampFormat>MMM dd, yyyy hh:mm:ss a</timeStampFormat>

        <!-- settings to change component location -->
        <!-- custom javascript -->
        <js>
            <!--      <![CDATA[-->

            <!--        $(document).ready(function() {-->
            <!--            $('.category-container').hide();-->
            <!--        });-->
            <!--     $('.card-title').each(function(){-->
            <!--         if ($(this).text()=='Scenarios'){-->
            <!--          $(this).text('Test Cases');-->
            <!--          }-->
            <!--        });-->

            <!--        $("small[data-tooltip]").text(function() {-->
            <!--       return $(this).text().replace("scenarios passed", "testcase passed");-->
            <!--        });-->

            <!--         $("small[data-tooltip]").text(function() {-->
            <!--       return $(this).text().replace("scenarios failed", "testcase failed");-->
            <!--        });-->


            <!--]]>-->
        </js>

        <!-- custom styles -->
        <css>
            <!--      <![CDATA[-->
            <!--      -->
            <!--      ]]>-->
        </css>

    </configuration>
</extentreports>